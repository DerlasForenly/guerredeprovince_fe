import { createTheme } from '@mui/material/styles';

export const mdTheme = createTheme({
  palette: {
    primary: {
      main: '#a67d0c',
    },
    secondary: {
      main: '#a67d0c',
    },
  },
});